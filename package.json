{
  "name": "roxstar",
  "version": "1.0.0",
  "private": true,
  "description": "Roxstar spin-wheel monorepo. Root scripts delegate to the server package for deploy targets like Render.",
  "scripts": {
    "build": "cd server && npm install && npx --yes prisma generate",
    "start": "cd server && npx --yes tsx src/index.ts",
    "postinstall": "echo 'Root install complete. Server deps are installed by the build script.'"
  }
}
